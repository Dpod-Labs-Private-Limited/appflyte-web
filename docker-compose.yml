services:
  appflyte-web-service-test:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3007:3000"
    env_file:
      - .env
    networks:
      - appflyte

networks:
  appflyte:
      external: true