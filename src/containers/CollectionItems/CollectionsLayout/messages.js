/*
 * CollectionsLayout Messages
 *
 * This contains all the text for the CollectionsLayout container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CollectionsLayout';

export default defineMessages({
  addItem: {
    id: `${scope}.addItem`,
    defaultMessage: 'Add Item',
  },
  save: {
    id: `${scope}.save`,
    defaultMessage: 'Save',
  },
  cancel: {
    id: `${scope}.cancel`,
    defaultMessage: 'Cancel',
  },
});
