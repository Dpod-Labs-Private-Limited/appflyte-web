/*
 * ImageCropperEnhanced Messages
 *
 * This contains all the text for the ImageCropperEnhanced component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ImageCropperEnhanced';

export default defineMessages({  
  cancelBtn: {
    id: `${scope}.cancelBtn`,
    defaultMessage: 'Cancel',
  },
  saveBtn: {
    id: `${scope}.saveBtn`,
    defaultMessage: 'OK ',
  },
});
