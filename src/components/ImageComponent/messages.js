/*
 * ImageComponent Messages
 *
 * This contains all the text for the ImageComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ImageComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ImageComponent component!',
  },
});
