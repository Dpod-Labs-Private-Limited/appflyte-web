/*
 * DrawerListCollection Messages
 *
 * This contains all the text for the DrawerListCollection component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DrawerListCollection';

export default defineMessages({
  collections: {
    id: `${scope}.collections`,
    defaultMessage: 'COLLECTIONS',
  },
});
