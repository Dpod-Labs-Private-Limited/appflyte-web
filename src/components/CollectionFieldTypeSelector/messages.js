/*
 * CollectionFieldTypeSelector Messages
 *
 * This contains all the text for the CollectionFieldTypeSelector component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CollectionFieldTypeSelector';

export default defineMessages({
  heading: {
    id: `${scope}.heading`,
    defaultMessage: 'SELECT A FIELD TYPE',
  },
  cancelBtn: {
    id: `${scope}.cancelBtn`,
    defaultMessage: 'Cancel',
  },
});
